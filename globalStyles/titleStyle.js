import { StyleSheet } from 'react-native'

export const titleStyle = StyleSheet.create({
    container: {
        paddingVertical: 20,
    },
    text: {
        fontSize: 30,
        fontWeight: 'bold',
        color: '#444',
        left: '15%',
    }
})